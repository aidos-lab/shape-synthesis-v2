{"cmd": "import pyvista as pv\nimport numpy as np\n\n\n# Seed rng for reproducibility\nrng = np.random.default_rng(seed=0)\npoints = rng.random((10, 3))\n\npv.plot(points)", "cmd_opts": " --cell_id=NONE -s", "import_complete": 1, "terminal": "nvimterm"}