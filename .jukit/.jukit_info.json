{"cmd": "\nrecon_plot = torch.tensor(density)\nrecon_plot /= recon_plot.max()\nrecon_plot[recon_plot < 0.8] = 0.0\n\nx_plot = (x.numpy() + 1) * (RESOLUTION / 2)\n\n# Create a PyVista grid\nplotter = pv.Plotter()\nplotter.add_volume(\n    recon_plot.cpu().numpy(),\n    cmap=\"viridis\",\n    opacity=\"sigmoid\",\n)\n\nplotter.add_points(\n    x_plot,\n    render_points_as_spheres=True,\n    point_size=5,\n    color=\"red\",\n    show_scalar_bar=False,\n)\n\nplotter.show()", "cmd_opts": " --cell_id=2vaJ8BMbLs -s", "import_complete": 1, "terminal": "nvimterm"}